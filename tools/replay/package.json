{
  "name": "@exxeed/replay",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Replay harness. Pipes a recording through the engine on a virtual clock and emits a timeline.",
  "bin": {
    "exxeed-replay": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc --build",
    "start": "tsc --build && node ./dist/cli.js"
  },
  "dependencies": {
    "@exxeed/core": "workspace:*",
    "@exxeed/repo": "workspace:*",
    "@exxeed/telemetry": "workspace:*"
  }
}
