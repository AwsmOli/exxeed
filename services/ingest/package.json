{
  "name": "@exxeed/ingest",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Video ingest pipeline. Standalone offline CLI — never bundled into the app. Stub until M5.",
  "bin": {
    "exxeed-ingest": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc --build",
    "start": "tsc --build && node ./dist/cli.js"
  },
  "dependencies": {
    "@exxeed/core": "workspace:*",
    "@exxeed/repo": "workspace:*"
  }
}
